// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init


//delete the balance 
generator trading_client {
  provider = "prisma-client-js"
}

datasource trading {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Trader {
  id           String        @id @default(uuid())
  address      String        @unique
  // kycStatus Boolean @default(false)
  accounts     Account[]
  orders       Order[]
  transactions Transaction[]
}

model Account {
  id         String  @id @default(uuid())
  traderId   String
  trader     Trader  @relation(fields: [traderId], references: [id])
  currency   String 
  locked     BigInt  @default(0)
  available  BigInt  @default(0)
  
  @@unique([traderId, currency]) //compound IDs&unique constraints
}

model Order {
  id            String   @id @default(uuid())
  traderId      String
  trader        Trader   @relation(fields: [traderId], references: [id])
  sell_currency String
  buy_currency  String
  amount        BigInt
  price         BigInt
  created       DateTime @default(now())
  order_status  String
  proof         String?
  filled        BigInt    @default(0)

  buyTrades     Trade[] @relation("BuyTrades")
  sellTrades    Trade[] @relation("SellTrades")
}

model Transaction {
  id         String   @id @default(uuid())
  type       String
  traderId   String
  trader     Trader   @relation(fields: [traderId], references: [id])
  token      String
  rawAmount     BigInt   // <-- exact on-chain value (from event.params.amount)
  humanAmount   String   // <-- ethers.formatUnits() output for display
  proof      String?
  txHash     String? @unique
  status     String
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}

model Trade {
  id         String   @id @default(uuid())
  buyOrderId   String
  sellOrderId  String
  buyOrder   Order   @relation("BuyTrades", fields: [buyOrderId], references: [id])
  sellOrder  Order  @relation("SellTrades", fields: [sellOrderId], references: [id])
  amount        BigInt
  price         BigInt
  executedAt    DateTime @default(now())
  txHash     String? @unique
}

// enum order_status{
//   PENDING
//   FILLED
//   PARTIAL
//   CANCELED
// }