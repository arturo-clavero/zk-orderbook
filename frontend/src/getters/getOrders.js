function getOrders(account) {
  //fetch from backend!
  //format orders data
  const orders = [];
  return orders;
}

function getFilledOrders(account) {
  const orders = getOrders(account);
  //filter orders!
  return orders;
}

function getPartialOrders(account) {
  const orders = getOrders(account);
  //filter orders!
  return orders;
}

function getPendingOrders(account) {
  const orders = getOrders(account);
  //filter orders!
  return orders;
}

export { getOrders, getFilledOrders, getPartialOrders, getPendingOrders };
